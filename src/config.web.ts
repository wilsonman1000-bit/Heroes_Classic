// Toggle to disable the legacy simple combat mode (kept for compatibility / reactivation).
// Set to `false` to make `showCombat` a no-op and prevent it from being launched from the UI.
export const ENABLE_SIMPLE_COMBAT = false;
